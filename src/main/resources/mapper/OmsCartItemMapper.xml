<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 指明当前xml对应的Mapper -->
<mapper namespace="com.luo.imall.web.dao.OmsCartItemDao">

   <select id="getCart" parameterType="com.luo.imall.web.entity.OmsCartItem" resultType="com.luo.imall.web.entity.OmsCartItem">
       select * from oms_cart_item where member_id = #{memberid};
   </select>

    <select id="getCartByName" parameterType="String" resultType="com.luo.imall.web.entity.OmsCartItem">
       select * from oms_cart_item where member_id in (select id from ums_member where username = #{name});
    </select>
</mapper>
